const listaAlumnos = ["gonzalo", "pablo", "micaela", "carola"]

for (let i = 0; i < listaAlumnos.length; i = i + 1) {
    let alumno = listaAlumnos[i]
    console.log("Buenas noches, mi nombre es " + alumno)
}